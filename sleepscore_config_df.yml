# User config for `sleepscore` call"""

# Path to Dataset
binPath: "/Users/tom/data/neuropixels/11-25-2019_g2/10-25-2019_g2_imec0/10-25-2019_g2_t0.imec0.lf.bin"
datatype: 'SGLX'

# Downsampling frequency
downSample: 100.0  # (Hz)

# Duration of the segment of data loaded
tStart: 0.0  # 0 (s)
tEnd: null  # (s) / end of recording if None

# Loaded channels
chanList: [0] # All channels if `None`
chanListType: 'indices'  # How are values in `chanList` interpreted? See doc
chanLabelsMap: null  # Map channel relabelling (keys are values in `chanList`)

# Unit of returned data
unit: 'uV'  # 'uV' or 'mV'

# Arguments passed to the `Sleep` GUI
kwargs_sleep: {
  downsample: null,
}
